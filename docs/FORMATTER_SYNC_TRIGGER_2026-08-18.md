# Hosted formatter and ledger sync trigger

This temporary marker triggers the one-shot hosted Flutter stable formatter and additive reliability-ledger synchronization. The workflow removes this marker and itself in the generated commit.
